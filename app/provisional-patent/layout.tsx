import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Provisional Patent Registration in USA, USA | Online Lookup",
  description: "Protect your invention with attorney-backed provisional patent registration in USA, USA. Fast online lookup and filing support.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
