import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Advance Health Care Directive in USA, USA",
  description: "Create an advance or Catholic health care directive in USA, USA, with attorney-guided support and protect your medical wishes!",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
