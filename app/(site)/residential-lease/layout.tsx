import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Online Residential Lease Agreement in USA, USA",
  description: "Create a residential lease agreement online in USA, USA. Secure, customizable, and legally compliant forms for landlords and tenants.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
