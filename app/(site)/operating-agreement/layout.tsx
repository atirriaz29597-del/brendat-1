import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create LLC Operating Agreement in USA | Top USA Attorneys",
  description: "Protect your business with custom LLC, Corporation Operating Agreement in USA, USA, tailored to your company's needs.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
