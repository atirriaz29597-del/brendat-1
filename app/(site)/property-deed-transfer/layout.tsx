import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Trusted Property Deed Transfer Lawyers in USA, USA",
  description: "Need a property deed transfer in USA? Our experienced real estate attorneys make the process smooth, secure, and compliant with USA law.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
