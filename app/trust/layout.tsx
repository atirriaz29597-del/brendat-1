import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Trusted Living Trust Lawyers in USA, USA | Top Attorneys",
  description: "Work with experienced living trust lawyers in USA, USA. We help you avoid probate, protect assets, & plan for future with confidence.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
