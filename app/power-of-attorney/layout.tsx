import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Consult Trusted Power of Attorney Lawyers in USA, USA",
  description: "Need a Power of Attorney in USA? Consult trusted USA lawyers to draft, review, and update your POA with confidence and legal precision.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
