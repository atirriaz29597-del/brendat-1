import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply for Business License in USA, USA | Top-rated Attorneys",
  description: "Need a USA business license? Our USA attorneys ensure comprehensive lookup, smooth filing, and full compliance with state laws. Apply Now!",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
