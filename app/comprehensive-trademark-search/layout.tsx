import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Attorney-Backed Trademark Registration Search in USA, USA",
  description: "Start your trademark search in USA with attorney support. Comprehensive checks, filing guidance, and brand protection you can trust.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
