import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Online Trademark Registration Services in USA, USA",
  description: "Looking for online trademark registration in USA? Get attorney-backed services to secure your business name, logo, or slogan.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
