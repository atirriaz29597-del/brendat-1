import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Top-rated Registered Agent Services in USA, USA",
  description: "Looking for the best registered agent in USA? Brendat provides dependable service, fast notifications, and complete compliance support.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
